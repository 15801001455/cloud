spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/world?userUnicode=true&characterEncoding=utf-8&userSSL=false&serverTimezone=UTC
      username: root
      password: root
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.jdbc.Driver
mybatis:
  type-aliases-package: com.funtl.spring.boot.druid.entity
  mapper-locations: classpath:mapper/*.xml